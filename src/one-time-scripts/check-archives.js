const {
    firestore,
    firebase,
} = require('../lib/setupFirebase.js');

// firestore.collection('archived_town_halls')
//     .get()
//     .then((querySnapshot) => {
//         querySnapshot.forEach((doc) => {
//             const th = doc.data();
//             if(!th.timeZone) {
//                 console.log(th.timeStart, th.eventId)
//             }
        
//         })
        
//     })

